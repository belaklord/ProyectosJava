package bloque2;
//SENTENCIAS SELECTIVAS (IF Y ELSE)//

public class sentenciasSelectivas {
	public static void main(String[] args){
		
		int x=5;
		
		if (x > 5) { 								// "==" (igual que), "<" (menor que), ">" (mayor que), ">=", "<=",
													// "!=" (distinto de).
			System.out.println ("es cierto");
		}
			else {
				System.out.println (" es falso");
			}
		}
	}